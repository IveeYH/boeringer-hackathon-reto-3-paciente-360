runtime: python312
env: flex

entrypoint: streamlit run web/main.py --browser.serverAddress 0.0.0.0 --server.port 8080 

instance_class: F1

env_variables:
  GOOGLE_APPLICATION_CREDENTIALS: $GCP_CREDENTIALS_PATH
  POSTGRES_USER: $POSTGRES_USER
  POSTGRES_PASS: $POSTGRES_PASS
